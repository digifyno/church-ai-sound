ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
FLASK_SECRET_KEY=change-me-to-a-random-string
# Mixer network address (defaults shown)
# MIXER_IP=192.168.8.18
# MIXER_PORT=10024
# WebSocket CORS origins (comma-separated). Defaults to localhost only.
# Add your LAN IP to access the dashboard from other devices on the network.
# CORS_ORIGINS=http://192.168.8.100:5050
